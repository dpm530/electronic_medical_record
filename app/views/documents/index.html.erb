<div class="container-fluid py-2">
   <div class="container-fluid bg-white border py-4">
      <div class="row">
         <div class="col-md-6">
            <h4>Files for this Patient:</h4>
         </div>
         <div class="col-md-6">
            <a href="/patients/<%= @patient.id %>/documents/new" class="btn btn-outline-secondary float-right">Upload Patient Files</a>
         </div>
      </div>
      <br>
      <div class="row">
         <div class="col-md-12">
            <table class="table table-bordered table-hover">
               <thead>
                  <tr>
                     <th>Document Name</th>
                     <th>Date</th>
                     <th>Author</th>
                     <th>file</th>
                  </tr>
               </thead>
               <tbody>
                  <% @documents.each do |document| %>
                     <tr>
                        <td><%= document.name %></td>
                        <td><%= document.created_at %></td>
                        <td><%= document.user.first_name %></td>
                        <td><%= document.file[0] %></td>
                        <td><%= link_to 'Show', [document.patient, document] %></td>
                        <td><%= link_to 'Edit', edit_patient_document_path(document.patient, document) %></td>
                        <td><%= link_to 'Destroy', [document.patient, document], method: :delete, data: { confirm: 'Are you sure?' } %></td>
                     </tr>
                  <% end %>
               </tbody>
            </table>
         </div>
      </div>
   </div>
</div>
